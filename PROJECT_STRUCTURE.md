# 📋 Структура проекта Admin Panel

## 🎯 Общая информация

**Название проекта:** Admin Panel  
**Версия:** 0.1.0  
**Тип:** SaaS система управления с мультитенантностью  
**Framework:** Next.js 16.1.1 (App Router)  
**Язык:** TypeScript 5.9.3  
**База данных:** PostgreSQL + Prisma ORM 7.1.0

---

## 📁 Структура файлов и директорий

```
admin-panel/
│
├── 📂 app/                          # Next.js App Router
│   ├── globals.css                  # Глобальные стили
│   ├── layout.tsx                   # Корневой layout
│   ├── page.tsx                     # Главная страница
│   ├── providers.tsx                # React Query и другие провайдеры
│   ├── favicon.ico                  # Иконка сайта
│   │
│   ├── 📂 admin/                    # Админ-панель (защищенная зона)
│   │   ├── layout.tsx               # Layout админ-панели
│   │   ├── page.tsx                 # Dashboard админки
│   │   ├── 📂 devices/              # Управление устройствами
│   │   │   └── page.tsx
│   │   ├── 📂 inventory/            # Управление инвентарем
│   │   │   └── page.tsx
│   │   ├── 📂 orders/               # Управление заказами
│   │   │   └── page.tsx
│   │   ├── 📂 products/             # Управление товарами
│   │   │   └── page.tsx
│   │   └── 📂 users/                # Управление пользователями
│   │       ├── page.tsx             # Список пользователей
│   │       └── 📂 [id]/             # Детали пользователя
│   │
│   ├── 📂 api/                      # API Routes (REST API)
│   │   ├── 📂 auth/                 # Аутентификация
│   │   │   ├── 📂 login/            # POST /api/auth/login
│   │   │   ├── 📂 logout/           # POST /api/auth/logout
│   │   │   ├── 📂 logout-all/       # POST /api/auth/logout-all
│   │   │   ├── 📂 refresh/          # POST /api/auth/refresh
│   │   │   └── 📂 register/         # POST /api/auth/register
│   │   ├── 📂 me/                   # Текущий пользователь
│   │   │   └── route.ts             # GET /api/me
│   │   ├── 📂 orders/               # CRUD заказов
│   │   │   └── route.ts             # GET, POST /api/orders
│   │   ├── 📂 products/             # CRUD товаров
│   │   │   ├── route.ts             # GET, POST /api/products
│   │   │   └── 📂 [id]/             # GET, PUT, DELETE /api/products/:id
│   │   ├── 📂 sessions/             # Управление сессиями
│   │   │   ├── route.ts             # GET /api/sessions
│   │   │   ├── 📂 delete/           # DELETE /api/sessions/delete
│   │   │   └── 📂 delete-others/    # DELETE /api/sessions/delete-others
│   │   └── 📂 users/                # CRUD пользователей
│   │       ├── route.ts             # GET, POST /api/users
│   │       ├── 📂 [id]/             # GET, PUT, DELETE /api/users/:id
│   │       └── 📂 delete-many/      # DELETE /api/users/delete-many
│   │
│   ├── 📂 login/                    # Страница входа
│   │   └── page.tsx
│   │
│   └── 📂 products/                 # Публичные страницы товаров
│       ├── page.tsx                 # Каталог товаров
│       ├── 📂 [id]/                 # Детали товара
│       │   └── page.tsx
│       └── 📂 create/               # Создание товара
│           └── page.tsx
│
├── 📂 prisma/                       # Prisma ORM
│   ├── schema.prisma                # Схема базы данных
│   ├── prisma-client.ts             # Инстанс Prisma Client
│   ├── constants.ts                 # Константы для сидов
│   ├── seed.ts                      # Скрипт заполнения БД
│   └── 📂 migrations/               # Миграции БД
│       ├── migration_lock.toml
│       └── 📂 [timestamp]_[name]/
│
├── 📂 shared/                       # Переиспользуемый код
│   │
│   ├── 📂 api/                      # React Query хуки
│   │   └── 📂 users/
│   │       ├── use-user-mutations.ts    # Мутации (create, update, delete)
│   │       └── use-users-query.ts       # Запросы (список пользователей)
│   │
│   ├── 📂 components/               # React компоненты
│   │   ├── index.ts
│   │   ├── 📂 shared/               # Бизнес-компоненты
│   │   │   ├── index.ts
│   │   │   └── 📂 orders/
│   │   │       └── DeleteOrderDialog.tsx
│   │   └── 📂 ui/                   # UI компоненты (Radix UI)
│   │       ├── button.tsx
│   │       ├── dialog.tsx
│   │       ├── input.tsx
│   │       └── ... (другие компоненты)
│   │
│   ├── 📂 context/                  # React Context
│   │   └── AuthContext.tsx          # Контекст аутентификации
│   │
│   ├── 📂 hooks/                    # Кастомные хуки
│   │   ├── index.ts
│   │   ├── useOrderFilters.ts       # Фильтрация заказов
│   │   └── useOrders.ts             # Работа с заказами
│   │
│   ├── 📂 lib/                      # Утилиты и вспомогательные функции
│   │   ├── auth-tokens.ts           # Работа с JWT токенами
│   │   ├── calculate-price.ts       # Расчет цен
│   │   ├── delete-others.ts         # Удаление других сессий
│   │   ├── deleteOrder.ts           # Удаление заказа
│   │   ├── device-id.ts             # Генерация ID устройства
│   │   ├── generate-temp-password.ts # Генерация временных паролей
│   │   ├── get-browser-icon.tsx     # Иконки браузеров
│   │   ├── get-device-icon.tsx      # Иконки устройств
│   │   ├── get-geo.ts               # Геолокация по IP
│   │   ├── get-user.ts              # Получение данных пользователя
│   │   ├── getBaseUrl.ts            # Базовый URL
│   │   ├── log-activity.ts          # Логирование действий
│   │   ├── logout-all.ts            # Выход со всех устройств
│   │   ├── logout.ts                # Выход из системы
│   │   ├── parse-user-agent.ts      # Парсинг User-Agent
│   │   ├── rate-limit.ts            # Rate limiting (Upstash Redis)
│   │   ├── refresh-auth.ts          # Обновление токенов
│   │   ├── sort-users.ts            # Сортировка пользователей
│   │   ├── utils.ts                 # Общие утилиты (cn и др.)
│   │   ├── 📂 types/                # Типы для lib
│   │   └── 📂 validation/           # Zod схемы валидации
│   │
│   ├── 📂 store/                    # Zustand стейт-менеджмент
│   │   └── 📂 users/
│   │
│   └── 📂 types/                    # TypeScript типы
│       ├── activity-log.ts          # Типы логов активности
│       ├── device-session.ts        # Типы сессий устройств
│       └── user-row.ts              # Типы строк пользователей
│
├── 📂 @types/                       # Глобальные типы
│   ├── base.types.ts                # Базовые типы
│   ├── fittings.types.ts            # Типы фурнитуры
│   ├── next-auth.d.ts               # Расширение типов NextAuth
│   ├── prisma.ts                    # Типы Prisma
│   ├── product.types.ts             # Типы продуктов
│   └── youkassa.ts                  # Типы ЮКассы
│
├── 📂 public/                       # Статические файлы
│
├── 📄 package.json                  # Зависимости и скрипты
├── 📄 tsconfig.json                 # Конфигурация TypeScript
├── 📄 next.config.ts                # Конфигурация Next.js
├── 📄 tailwind.config.ts            # Конфигурация Tailwind CSS
├── 📄 postcss.config.mjs            # Конфигурация PostCSS
├── 📄 components.json               # Конфигурация shadcn/ui
├── 📄 eslint.config.mjs             # Конфигурация ESLint
├── 📄 prisma.config.ts              # Конфигурация Prisma
├── 📄 proxy.ts                      # Прокси-конфигурация
├── 📄 README.md                     # Документация
├── 📄 AUDIT_SAAS_ROADMAP.md         # Роадмап SaaS аудита
└── 📄 next-env.d.ts                 # Типы Next.js
```

---

## 🗄️ База данных (Prisma Schema)

### Основные модели:

#### **Tenant** (Мультитенантность)

- `id`, `name`, `createdAt`
- Связи: `users[]`, `products[]`, `orders[]`

#### **User** (Пользователи)

- `id`, `fullName`, `email`, `passwordHash`, `role`, `tenantId`
- `verified`, `provider`, `providerId`, `mustChangePassword`, `banned`
- Роли: `USER`, `MANAGER`, `ADMIN`
- Связи: `sessions[]`, `activityLogs[]`, `orders[]`, `cart`, `tenant`

#### **Session** (Сессии пользователей)

- `id`, `userId`, `refreshToken`, `userAgent`, `ip`, `deviceId`, `expiresAt`
- Связи: `user`

#### **ActivityLog** (Логи активности)

- `id`, `userId`, `action`, `ip`, `userAgent`, `createdAt`
- Связи: `user` (cascade delete)

#### **Product** (Товары)

- `id`, `name`, `imageUrl`, `tenantId`, `categoryId`
- Связи: `items[]`, `ingredients[]`, `category`, `tenant`

#### **ProductItem** (Варианты товаров)

- `id`, `price`, `productId`, `productColor`, `productLength`, `productMaterials`
- `productShape`, `productSizes`, `productThickness`, `pvcSize`, `steelSize`
- Материалы: `STEEL`, `PVC`, `ALUMINIUM`, `PLASTIC`, `RUBBER`
- Связи: `product`, `cartItem[]`

#### **Category** (Категории)

- `id`, `name`, `createdAt`, `updatedAt`
- Связи: `products[]`

#### **Ingredient** (Ингредиенты/Дополнения)

- `id`, `name`, `price`, `imageUrl`
- Связи: `products[]`, `cartItem[]`

#### **Cart** (Корзины)

- `id`, `userId`, `token`, `totalAmount`
- Связи: `user`, `items[]`

#### **CartItem** (Элементы корзины)

- `id`, `cartId`, `productItemId`, `quantity`
- Связи: `cart`, `productItem`, `ingredients[]`

#### **Order** (Заказы)

- `id`, `userId`, `tenantId`, `token`, `totalAmount`, `status`, `paymentId`
- `items` (JSON), `fullName`, `email`, `phone`, `address`, `comment`
- Статусы: `PENDING`, `SUCCEEDED`, `CANCELLED`
- Связи: `user`, `tenant`

#### **VerificationCode** (Коды подтверждения)

- `id`, `userId`, `code`, `expiresAt`
- Связи: `user`

#### **Story** & **StoryItem** (Истории)

- Для отображения историй (stories) на фронтенде

---

## 🛠️ Технологический стек

### Frontend

- **React** 19.2.3
- **Next.js** 16.1.1 (App Router)
- **TypeScript** 5.9.3
- **Tailwind CSS** 4.0
- **Radix UI** (headless UI components)
- **Lucide React** 0.560.0 (иконки)

### State Management & Data Fetching

- **React Query** (@tanstack/react-query) 5.90.13
- **Zustand** 5.0.9
- **React Hook Form** 7.69.0

### Backend & Database

- **Prisma ORM** 7.1.0
- **PostgreSQL** (через @prisma/adapter-pg)
- **pg** 8.16.3 (PostgreSQL driver)

### Authentication & Security

- **JWT** (jsonwebtoken 9.0.3)
- **bcrypt** 6.0.0 / bcryptjs 3.0.3
- **Rate Limiting** (@upstash/ratelimit + redis)

### Validation & Forms

- **Zod** 4.2.1
- **@hookform/resolvers** 5.2.2

### UI/UX

- **React Hot Toast** 2.6.0
- **Sonner** 2.0.7
- **NextJS TopLoader** 3.9.17
- **date-fns** 4.1.0
- **react-day-picker** 9.12.0

### Development Tools

- **ESLint** 9
- **Prettier** (через ESLint)
- **TypeScript** 5.9.3
- **tsx** 4.21.0 (для запуска TypeScript)
- **concurrently** 9.2.1

---

## 📜 Доступные скрипты

```bash
# Разработка
npm run dev              # Запуск dev сервера (localhost:3000)

# Сборка
npm run build            # Production build
npm run start            # Запуск production сервера

# Линтинг
npm run lint             # Проверка кода ESLint

# Prisma
npm run prisma:generate  # Генерация Prisma Client
npm run prisma:migrate   # Создание и применение миграций
npm run prisma:studio    # Открыть Prisma Studio (GUI)
npm run prisma:reset     # Сброс БД
npm run prisma:push      # Push schema без миграций
npm run prisma:preview   # Pull schema из БД

# Seeding
npm run seed             # Заполнение БД тестовыми данными
```

---

## 🔐 Система аутентификации

### Основные возможности:

- ✅ JWT токены (access + refresh)
- ✅ Управление сессиями (несколько устройств)
- ✅ Логирование активности (ActivityLog)
- ✅ Rate Limiting (Upstash Redis)
- ✅ Device fingerprinting
- ✅ IP трекинг
- ✅ User-Agent парсинг
- ✅ Геолокация по IP
- ✅ Выход со всех устройств
- ✅ Обязательная смена пароля (mustChangePassword)
- ✅ Блокировка пользователей (banned)

### Роли пользователей:

- **USER** — обычный пользователь
- **MANAGER** — менеджер
- **ADMIN** — администратор

---

## 🏢 Мультитенантность

Проект поддерживает мультитенантность через модель **Tenant**:

- Каждый пользователь привязан к тенанту (`tenantId`)
- Товары и заказы изолированы по тенантам
- Возможность создания нескольких независимых инстансов

---

## 🧩 Архитектурные особенности

### Паттерны:

- **App Router** (Next.js 15+) вместо Pages Router
- **Server Components** по умолчанию
- **API Routes** для REST API
- **React Query** для кеширования и синхронизации данных
- **Zustand** для глобального стейта
- **Prisma** для типобезопасной работы с БД
- **Zod** для валидации на клиенте и сервере

### Структура кода:

- Разделение на `app/` (роуты) и `shared/` (переиспользуемый код)
- Компоненты разделены на `ui/` (чистые UI) и `shared/` (бизнес-логика)
- Централизованная валидация в `shared/lib/validation/`
- Типизация вынесена в `@types/` и `shared/types/`

---

## 📊 Основные функции системы

### Администрирование:

- ✅ Управление пользователями (CRUD, блокировка, смена ролей)
- ✅ Управление товарами и категориями
- ✅ Управление заказами (статусы, комментарии)
- ✅ Просмотр устройств и сессий
- ✅ Логи активности пользователей
- ✅ Инвентаризация

### Пользовательские функции:

- ✅ Регистрация и вход
- ✅ Каталог товаров
- ✅ Корзина покупок
- ✅ Оформление заказов
- ✅ История заказов
- ✅ Управление сессиями (выход с других устройств)

### Интеграции:

- ✅ ЮКасса (платежи)
- ✅ Upstash Redis (rate limiting)
- ✅ IP Geolocation

---

## 🔮 Планы развития (из AUDIT_SAAS_ROADMAP.md)

### Фаза 1: Мультитенантность

- ✅ Модель Tenant
- ⏳ Регистрация организаций
- ⏳ Управление командами
- ⏳ Приглашения (InvitationToken)

### Фаза 2: Подписки и биллинг

- ⏳ Модель Subscription
- ⏳ Модель Invoice
- ⏳ Интеграция с Stripe/Paddle
- ⏳ Тарифные планы

### Фаза 3: Расширенная аналитика

- ⏳ Dashboard с метриками
- ⏳ Отчеты по тенантам
- ⏳ Экспорт данных

---

## 📝 Примечания

- Проект использует **Next.js 16** с **React 19** (новейшие версии)
- База данных **PostgreSQL** с **Prisma ORM 7**
- UI построен на **Radix UI** + **Tailwind CSS 4**
- Аутентификация полностью кастомная (без NextAuth)
- Поддержка **TypeScript** на всех уровнях
- Rate Limiting через **Upstash Redis**

---

## 📞 Контакты и поддержка

Проект находится в активной разработке. Для вопросов и предложений обращайтесь к команде разработки.

---

**Дата создания документа:** 28 января 2026 г.  
**Версия проекта:** 0.1.0
